%h1 Listing products

%table.table.table-striped
  %tr
    %th Title
    %th Description
    %th Price
    %th{:width => "25%"}

  - products.each do |product|
    %tr
      %td= product.title
      %td= product.description
      %td= product.price
      %td
        .pull-right
          = link_to 'Show', category_product_path(product.category, product), class: ['btn btn-default btn-success']
          - if current_user && product.user == current_user
            = link_to 'Edit', edit_category_product_path(product.category, product), class: ['btn btn-default btn-warning']
            = link_to 'Destroy', category_product_path(product.category, product), method: :delete, data: { confirm: 'Are you sure?' }, class: ['btn btn-default btn-danger']

%br
